// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  // i18n: {
  //   locales: ['en-US', 'zh-TW'],
  //   defaultLocale: 'en-US',
  // },
}

module.exports = nextConfig
